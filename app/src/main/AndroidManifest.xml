<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kam.mytourism">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TourismListActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".HundruFall" />
        <activity android:name=".DashamFall" />
        <activity android:name=".PahadiMandir"></activity>
        <activity android:name=".PatratuValley"></activity>
        <activity android:name=".DeoriMandir"></activity>
    </application>

</manifest>